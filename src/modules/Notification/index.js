import * as constants from 'modules/Notification/constants';
import * as actions from 'modules/Notification/actions';
import reducers from 'modules/Notification/reducers';
import * as selectors from 'modules/Notification/selectors';
import * as hooks from 'modules/Notification/hooks';

const moduleName = 'Notification';

export {
  constants,
  reducers,
  moduleName,
  actions,
  selectors,
  hooks,
};
