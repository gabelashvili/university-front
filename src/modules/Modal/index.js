import * as constants from 'modules/Modal/constants';
import * as actions from 'modules/Modal/actions';
import reducers from 'modules/Modal/reducers';
import * as selectors from 'modules/Modal/selectors';

const moduleName = 'Modal';

export {
  constants,
  reducers,
  moduleName,
  actions,
  selectors,
};
