import * as constants from 'modules/Register/constants';
import * as actions from 'modules/Register/actions';
import reducers from 'modules/Register/reducers';
import * as selectors from 'modules/Register/selectors';
import sagas from 'modules/Register/sagas';

const moduleName = 'Register';

export {
  constants,
  reducers,
  moduleName,
  actions,
  sagas,
  selectors,
};
